/* =========================================================
   RO LTDA — Sitio web v2
   Estilo consultora (inspirado en la estructura de referencia),
   con la paleta del logo de RO: ámbar/naranja, magenta y morado
   sobre un cubo/globo, texto de marca en negro.
   ========================================================= */
:root {
  --ro-amber: #FFB300;       /* dorado vívido (caras superiores del cubo) */
  --ro-amber-dark: #FA7606;  /* naranja vívido, tono de la imagen de referencia */
  --ro-amber-deep: #E8490A;  /* naranja-rojo profundo, para resplandores */
  --ro-magenta: #A31C90;     /* magenta con toque de morado, tono del lazo */
  --ro-magenta-deep: #6E0A63;/* magenta-morado oscuro, ajustado al mismo tono */
  --ro-purple: #8425F0;      /* morado vívido, complementa el degradado del cubo */
  --ro-purple-deep: #3E1278; /* morado profundo, para fondos oscuros */
  --ro-ink: #10082B;         /* navy-morado muy oscuro, para navbar/franjas oscuras */
  --ro-ink-soft: #1B0F3D;
  --ro-gray-100: #F6F5F8;
  --ro-gray-300: #E4E2EA;
  --ro-white: #FFFFFF;

  --ro-gradient: linear-gradient(120deg, var(--ro-amber) 0%, var(--ro-magenta) 50%, var(--ro-purple) 100%);
  /* Fondo oscuro de secciones (antes degradado "retazos"): ahora color sólido, sin mezcla de tonos */
  --ro-dark-gradient: var(--ro-ink);

  /* Color estandarizado de TODOS los botones sólidos del sitio (tono de "Contáctanos") */
  --ro-btn-gradient: linear-gradient(135deg, #6B0240 0%, #A5034F 45%, #93157F 75%, #7A1C8C 100%);
  --ro-btn-gradient-hover: linear-gradient(135deg, #5A0235 0%, #8F0244 45%, #7E1069 75%, #641877 100%);
  --ro-btn-shadow: rgba(163, 28, 144, 0.4);
}

* { scroll-behavior: smooth; }

body {
  font-family: "Manrope", "Segoe UI", system-ui, -apple-system, sans-serif;
  color: var(--ro-ink);
  background-color: var(--ro-white);
}

h1, h2, h3, h4, .navbar-brand {
  font-family: "Plus Jakarta Sans", "Manrope", "Segoe UI", system-ui, -apple-system, sans-serif;
  font-weight: 800;
  letter-spacing: -0.015em;
}

.text-gradient {
  color: var(--ro-magenta);
}
/* Dentro de los encabezados oscuros (Servicios, Galería, Contacto, 404),
   el título va todo en blanco, sin resaltar ninguna palabra en color */
.hero .text-gradient {
  color: var(--ro-white);
}

a { color: var(--ro-magenta); text-decoration: none; }
a:hover { color: var(--ro-purple); }

.eyebrow {
  text-transform: uppercase;
  letter-spacing: 0.08em;
  font-size: 1.2rem;
  font-weight: 700;
  color: var(--ro-amber-dark);
}
.eyebrow-hero {
  position: relative;
  display: inline-block;
  padding-bottom: 12px;
}
.eyebrow-hero::after {
  content: "";
  position: absolute;
  left: 1px;
  bottom: 0;
  width: 34px;
  height: 3px;
  border-radius: 2px;
  background: var(--ro-amber-dark);
}
.text-magenta-solid { color: var(--ro-magenta); }

/* ---------- Navbar ---------- */
.navbar-ro {
  background-color: var(--ro-ink);
  padding: 0.85rem 0;
  box-shadow: 0 2px 18px rgba(0, 0, 0, 0.25);
}
.navbar-ro .navbar-brand {
  color: var(--ro-white);
  display: flex;
  align-items: center;
  gap: 10px;
}
.navbar-ro .navbar-brand img {
  height: 52px;
  width: auto;
}
.navbar-ro .nav-link {
  position: relative;
  color: rgba(255, 255, 255, 0.85);
  font-weight: 600;
  font-size: 0.93rem;
  margin: 0 4px;
  padding-bottom: 6px;
}
.navbar-ro .nav-link:hover,
.navbar-ro .nav-link.active {
  color: var(--ro-amber-dark);
}
.navbar-ro .nav-link.active::after {
  content: "";
  position: absolute;
  left: 0;
  right: 0;
  bottom: -2px;
  height: 2px;
  border-radius: 2px;
  background: var(--ro-amber-dark);
}
.navbar-ro .dropdown-menu {
  border: none;
  border-radius: 14px;
  padding: 0.6rem;
  box-shadow: 0 20px 45px rgba(0, 0, 0, 0.3);
}
.navbar-ro .dropdown-item {
  border-radius: 8px;
  padding: 0.55rem 0.9rem;
  font-size: 0.9rem;
  font-weight: 600;
  color: var(--ro-ink);
}
.navbar-ro .dropdown-item:hover {
  background: var(--ro-gray-100);
  color: var(--ro-magenta);
}
.navbar-ro .navbar-toggler {
  border-color: rgba(255, 255, 255, 0.3);
}
.navbar-ro .navbar-toggler-icon {
  filter: invert(1);
}

.btn-ro {
  background: var(--ro-btn-gradient);
  border: none;
  color: var(--ro-white) !important;
  font-weight: 700;
  padding: 0.65rem 1.6rem;
  border-radius: 50px;
  transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
}
.btn-ro:hover {
  background: var(--ro-btn-gradient-hover);
  transform: translateY(-2px);
  box-shadow: 0 10px 24px var(--ro-btn-shadow);
  color: var(--ro-white) !important;
}
.btn-ro-solid {
  background: var(--ro-btn-gradient);
  border: none;
  color: var(--ro-white) !important;
  font-weight: 700;
  padding: 0.65rem 1.6rem;
  border-radius: 10px;
  transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
}
.btn-ro-solid:hover {
  background: var(--ro-btn-gradient-hover);
  transform: translateY(-2px);
  box-shadow: 0 10px 24px var(--ro-btn-shadow);
  color: var(--ro-white) !important;
}
.btn-ro-outline {
  background: transparent;
  border: 2px solid rgba(255, 255, 255, 0.5);
  color: var(--ro-white) !important;
  font-weight: 700;
  padding: 0.6rem 1.5rem;
  border-radius: 50px;
}
.btn-ro-outline:hover {
  background: var(--ro-white);
  color: var(--ro-ink) !important;
  border-color: var(--ro-white);
}
.btn-ro-outline-dark {
  background: transparent;
  border: 2px solid var(--ro-ink);
  color: var(--ro-ink) !important;
  font-weight: 700;
  padding: 0.6rem 1.5rem;
  border-radius: 50px;
}
.btn-ro-outline-dark:hover {
  background: var(--ro-ink);
  color: var(--ro-white) !important;
}
.btn-whatsapp-pill {
  background: var(--ro-btn-gradient);
  border: none;
  color: var(--ro-white) !important;
  font-weight: 700;
  font-size: 0.85rem;
  padding: 0.55rem 1.2rem;
  border-radius: 50px;
  display: inline-flex;
  align-items: center;
  transition: transform 0.2s ease, background 0.2s ease;
}
.btn-whatsapp-pill:hover {
  background: var(--ro-btn-gradient-hover);
  color: var(--ro-white) !important;
  transform: translateY(-1px);
}

/* ---------- Hero (oscuro, tipo consultora) ---------- */
.hero {
  position: relative;
  background: var(--ro-ink);
  color: var(--ro-white);
  padding: 5.5rem 0 4rem;
  overflow: hidden;
}
/* Misma textura de puntos que la sección "Nuestros servicios", para un fondo uniforme y sin degradado */
.hero::before {
  content: "";
  position: absolute;
  inset: 0;
  background-image: radial-gradient(rgba(255, 255, 255, 0.16) 1.6px, transparent 1.6px);
  background-size: 26px 26px;
  z-index: 0;
  pointer-events: none;
}
.hero > .container {
  position: relative;
  z-index: 1;
}
.hero h1 {
  font-size: clamp(2.1rem, 4.5vw, 3.4rem);
  line-height: 1.12;
}
.hero .lead {
  color: rgba(255, 255, 255, 0.78);
  font-size: 1.1rem;
}
.hero-pills {
  display: flex;
  flex-wrap: wrap;
  gap: 0.9rem;
  margin-top: 2.2rem;
}
.hero-pill {
  background: rgba(255, 255, 255, 0.06);
  border: 1px solid rgba(255, 255, 255, 0.14);
  border-radius: 16px;
  padding: 0.9rem 1.1rem;
  flex: 1 1 200px;
  display: flex;
  align-items: flex-start;
  gap: 0.7rem;
}
.hero-pill i {
  font-size: 1.3rem;
  color: var(--ro-amber);
}
.hero-pill .pill-title {
  font-weight: 700;
  font-size: 0.92rem;
  color: var(--ro-white);
}
.hero-pill .pill-text {
  font-size: 0.8rem;
  color: rgba(255, 255, 255, 0.65);
}
.hero-img-wrap {
  position: relative;
  text-align: center;
}
.hero-img-wrap img {
  max-width: 100%;
  border-radius: 20px;
  filter: drop-shadow(0 25px 45px rgba(0, 0, 0, 0.45));
}

/* ---------- Home Hero (a sangre, layout tipo plantilla de referencia) ---------- */
.home-hero {
  position: relative;
  padding: 0;
  background-color: var(--ro-gray-100);
  background-image: radial-gradient(rgba(20, 21, 26, 0.07) 1.5px, transparent 1.5px);
  background-size: 24px 24px;
  overflow: hidden;
}
.home-hero-flex {
  position: relative;
  z-index: 1;
  display: flex;
  align-items: stretch;
  min-height: 500px;
}
.home-hero-text-col {
  flex: 0 0 54%;
  max-width: 54%;
  display: flex;
  align-items: center;
  position: relative;
  z-index: 3;
}
.home-hero-text-inner {
  position: relative;
  max-width: 560px;
  margin-left: max(1rem, calc((100vw - 1140px) / 2 - 12px));
  padding: 1.8rem 3.5rem 1.8rem 0;
}
.home-hero h1 {
  color: var(--ro-ink);
  font-size: clamp(1.8rem, 2.6vw, 2.45rem);
  line-height: 1.18;
}
.home-hero .lead {
  color: #555a63;
  font-size: 0.98rem;
  line-height: 1.6;
}
.home-hero .btn {
  padding: 0.7rem 1.4rem;
  font-size: 0.95rem;
  white-space: nowrap;
}
.home-hero .btn-ro-outline-dark {
  border-radius: 10px;
  border: 1.5px solid var(--ro-gray-300);
  background: var(--ro-white);
  color: var(--ro-ink) !important;
}
.home-hero .btn-ro-outline-dark:hover {
  background: var(--ro-gray-100);
  border-color: var(--ro-gray-300);
  color: var(--ro-ink) !important;
}
.hero-dots {
  display: flex;
  align-items: center;
  gap: 0.4rem;
}
.hero-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--ro-gray-300);
}
.hero-dot.active {
  background: var(--ro-magenta);
  width: 22px;
  border-radius: 5px;
}

/* Columna de la foto: a sangre (edge-to-edge), como en la referencia.
   Sin tarjeta flotante ni bordes redondeados: la foto llena el alto
   completo de la fila y el "rayo" de marca cruza la costura entre el
   texto y la foto. */
.home-hero-media-col {
  flex: 1;
  position: relative;
  z-index: 2;
}
.home-hero-media-col .hero-img-wrap {
  position: relative;
  z-index: 2;
  height: 100%;
  overflow: hidden;
  isolation: isolate;
}
.home-hero-media-col .hero-img-wrap img {
  width: 100%;
  height: 100%;
  min-height: 460px;
  object-fit: cover;
  object-position: center 22%;
  display: block;
  border-radius: 0;
  transition: transform 0.6s ease;
}
.home-hero-media-col:hover .hero-img-wrap img {
  transform: scale(1.03);
}
/* Ligero degradado en la base de la foto para que las insignias resalten */
.hero-img-shade {
  position: absolute;
  inset: 0;
  z-index: 1;
  background: linear-gradient(0deg, rgba(7, 20, 46, 0.55) 0%, rgba(7, 20, 46, 0) 30%, rgba(7, 20, 46, 0) 100%);
  pointer-events: none;
}


/* Retícula de puntos decorativa junto al eyebrow, como en la referencia */
.hero-corner-dots {
  position: absolute;
  top: -34px;
  left: -6px;
  width: 60px;
  height: 60px;
  background-image: radial-gradient(#c9cdd6 1.8px, transparent 1.8px);
  background-size: 11px 11px;
  opacity: 0.7;
  pointer-events: none;
  z-index: 0;
}

/* El "lazo" de marca: UN solo SVG (bandas + fragmentos + pliegue) con su propio
   sistema de coordenadas. Se dimensiona por la ALTURA del hero y conserva su
   proporción (aspect-ratio + width:auto), de modo que ninguna pieza se
   deforma ni se desacomoda: solo escala. Queda anclado a la costura entre el
   texto y la foto (el 48.75% de su ancho cae a la izquierda de la costura). */
.hero-ribbon-svg {
  position: absolute;
  top: -6px;
  left: 0;
  height: calc(100% + 12px);
  width: auto;
  aspect-ratio: 400 / 606;
  transform: translateX(-48.75%);
  overflow: visible;
  z-index: 3;
  pointer-events: none;
}

.hero-badge-card {
  position: absolute;
  right: 24px;
  bottom: 24px;
  z-index: 4;
  background: rgba(7, 20, 46, 0.85);
  backdrop-filter: blur(6px);
  color: var(--ro-white);
  border-radius: 14px;
  padding: 0.85rem 1.15rem;
  display: flex;
  align-items: center;
  gap: 0.7rem;
  box-shadow: 0 16px 34px rgba(7, 20, 46, 0.4);
  border: 1px solid rgba(255, 255, 255, 0.08);
}
.hero-badge-card i {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 34px;
  height: 34px;
  flex: none;
  border-radius: 50%;
  font-size: 1.05rem;
  color: var(--ro-magenta);
  background: rgba(255, 255, 255, 0.08);
}
.hero-badge-card strong {
  display: block;
  font-size: 0.95rem;
  line-height: 1.2;
}
.hero-badge-card span {
  position: relative;
  display: inline-block;
  font-size: 0.76rem;
  color: rgba(255, 255, 255, 0.7);
  padding-bottom: 6px;
}
.hero-badge-card span::after {
  content: "";
  position: absolute;
  left: 0;
  bottom: 0;
  width: 22px;
  height: 2px;
  border-radius: 2px;
  background: var(--ro-amber-dark);
}


/* Escritorio medio: el lazo escala con el hero, así que se le da más aire al texto */
@media (min-width: 992px) and (max-width: 1199.98px) {
  .home-hero-text-col { flex-basis: 58%; max-width: 58%; }
  .home-hero-text-inner { max-width: 440px; padding-right: 2.6rem; }
}

/* Tablet y móvil: el hero se apila (texto arriba, foto abajo) y el lazo se
   conserva completo, escalado a la altura de la foto y pegado a su borde izquierdo. */
@media (max-width: 991.98px) {
  .home-hero-flex { flex-direction: column; min-height: 0; }
  .home-hero-text-col { flex: 1 1 auto; max-width: 100%; }
  .home-hero-text-inner { margin-left: 0; padding: 3rem 1.5rem 2rem; max-width: 100%; }
  .home-hero-media-col { height: 420px; width: 100%; }
  .home-hero-media-col .hero-img-wrap img { min-height: 0; }
  .hero-ribbon-svg { transform: translateX(-30%); }
  .hero-corner-dots { display: none; }
}
@media (max-width: 575.98px) {
  .home-hero-media-col { height: 340px; }
  .hero-ribbon-svg { transform: translateX(-38%); }
  .hero-badge-card { right: 12px; bottom: 12px; padding: 0.65rem 0.9rem; }
  .hero-badge-card span { display: none; }
}

/* ---------- Secciones ---------- */
section { padding: 5rem 0; }
.section-eyebrow { margin-bottom: 0.4rem; }
.section-title { font-size: 2rem; margin-bottom: 0.6rem; }
.section-subtitle { color: #6b6b6b; max-width: 680px; }
.bg-soft { background-color: var(--ro-gray-100); }
.bg-ink { background-color: var(--ro-ink); color: var(--ro-white); }
.bg-ink-band {
  position: relative;
  overflow: hidden;
  background: var(--ro-dark-gradient);
}
/* Textura de puntos, como "confeti" sutil sobre el fondo oscuro */
.bg-ink-band::before {
  content: "";
  position: absolute;
  inset: 0;
  background-image: radial-gradient(rgba(255, 255, 255, 0.16) 1.6px, transparent 1.6px);
  background-size: 26px 26px;
  z-index: 0;
  pointer-events: none;
}
/* Resplandores de color eliminados: el fondo ahora es un color sólido, sin mezcla de tonos */
.bg-ink-band > .container {
  position: relative;
  z-index: 1;
}

/* ---------- Tarjetas ---------- */
.ro-card {
  border: 1px solid var(--ro-gray-300);
  border-radius: 18px;
  padding: 1.9rem 1.6rem;
  height: 100%;
  background: var(--ro-white);
  transition: transform 0.25s ease, box-shadow 0.25s ease, border-color 0.25s ease;
}
.ro-card:hover {
  transform: translateY(-6px);
  box-shadow: 0 18px 36px rgba(20, 21, 26, 0.1);
  border-color: transparent;
}
.ro-card .icon-wrap {
  width: 52px;
  height: 52px;
  border-radius: 14px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--ro-btn-gradient);
  color: var(--ro-white);
  font-size: 1.4rem;
  margin-bottom: 1rem;
}

/* Tarjeta de categoría de servicio (portafolio) */
.category-card {
  border-radius: 18px;
  padding: 2rem 1.7rem;
  height: 100%;
  background: var(--ro-white);
  border: 1px solid var(--ro-gray-300);
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}
.category-card:hover {
  transform: translateY(-6px);
  box-shadow: 0 18px 36px rgba(20, 21, 26, 0.1);
}
.category-card .icon-wrap {
  width: 56px;
  height: 56px;
  border-radius: 16px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--ro-ink);
  color: var(--ro-amber);
  font-size: 1.5rem;
  margin-bottom: 1.1rem;
}
.category-card .badge-count {
  font-size: 0.75rem;
  font-weight: 700;
  color: var(--ro-magenta);
  background: rgba(214, 36, 140, 0.08);
  border-radius: 50px;
  padding: 0.25rem 0.7rem;
}

/* ---------- Tarjetas de servicio (banda oscura, ícono circular de color) ---------- */
.service-card {
  position: relative;
  overflow: hidden;
  border-radius: 16px;
  padding: 1.9rem 1.6rem;
  height: 100%;
  background: var(--ro-white);
  border-bottom: 4px solid transparent;
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}
.service-card:hover {
  transform: translateY(-6px) scale(1.01);
  box-shadow: 0 18px 36px rgba(0, 0, 0, 0.28);
}
/* "Retazo" circular de color, apenas visible, en la esquina de cada tarjeta */
.service-card::after {
  content: "";
  position: absolute;
  top: -38px;
  right: -38px;
  width: 100px;
  height: 100px;
  border-radius: 50%;
  opacity: 0.12;
  pointer-events: none;
  transition: opacity 0.25s ease, transform 0.25s ease;
}
.service-card:hover::after { opacity: 0.2; transform: scale(1.1); }
.service-card-icon {
  position: relative;
  z-index: 1;
  width: 54px;
  height: 54px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.4rem;
  color: var(--ro-white);
  margin-bottom: 1.1rem;
  box-shadow: 0 10px 20px -8px rgba(0, 0, 0, 0.35);
}
.service-card h5 { position: relative; z-index: 1; color: var(--ro-ink); }
.service-card p { position: relative; z-index: 1; }
.service-card-magenta { border-bottom-color: var(--ro-magenta); }
.service-card-magenta .service-card-icon { background: linear-gradient(135deg, var(--ro-magenta), var(--ro-magenta-deep)); }
.service-card-magenta::after { background: var(--ro-magenta); }
.service-card-amber { border-bottom-color: var(--ro-amber-dark); }
.service-card-amber .service-card-icon { background: linear-gradient(135deg, var(--ro-amber-dark), var(--ro-amber-deep)); }
.service-card-amber::after { background: var(--ro-amber-dark); }
.service-card-purple { border-bottom-color: var(--ro-purple); }
.service-card-purple .service-card-icon { background: linear-gradient(135deg, var(--ro-purple), var(--ro-purple-deep)); }
.service-card-purple::after { background: var(--ro-purple); }
.service-card-gold { border-bottom-color: var(--ro-amber); }
.service-card-gold .service-card-icon { background: linear-gradient(135deg, var(--ro-amber), var(--ro-amber-dark)); }
.service-card-gold::after { background: var(--ro-amber); }

/* ---------- Mini-tarjetas de valores ---------- */
.value-mini-card {
  border-radius: 14px;
  border: 1px solid var(--ro-gray-300);
  padding: 1.2rem 1rem;
  height: 100%;
  transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
}
.value-mini-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 14px 28px rgba(16, 8, 43, 0.1);
  border-color: transparent;
}
.value-mini-icon {
  width: 42px;
  height: 42px;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.1rem;
  color: var(--ro-white);
  margin-bottom: 0.8rem;
  box-shadow: 0 8px 16px -6px rgba(0, 0, 0, 0.4);
}
.value-mini-icon-magenta { background: linear-gradient(135deg, var(--ro-magenta), var(--ro-magenta-deep)); }
.value-mini-icon-amber { background: linear-gradient(135deg, var(--ro-amber-dark), var(--ro-amber-deep)); }
.value-mini-icon-purple { background: linear-gradient(135deg, var(--ro-purple), var(--ro-purple-deep)); }
.value-mini-icon-gold { background: linear-gradient(135deg, var(--ro-amber), var(--ro-amber-dark)); }

/* ---------- Panel de información de contacto (naranja) ---------- */
.contact-info-panel {
  position: relative;
  overflow: hidden;
  background: linear-gradient(155deg, var(--ro-amber) 0%, var(--ro-amber-dark) 55%, var(--ro-amber-deep) 100%);
  border-radius: 18px;
  padding: 1.8rem 1.6rem;
  height: 100%;
  color: var(--ro-white);
  box-shadow: 0 18px 34px -14px rgba(232, 73, 10, 0.55);
}
/* Anillo decorativo, como el detalle curvo de la referencia */
.contact-info-panel::before {
  content: "";
  position: absolute;
  right: -46px;
  bottom: -46px;
  width: 170px;
  height: 170px;
  border-radius: 50%;
  border: 2px solid rgba(255, 255, 255, 0.28);
  pointer-events: none;
}
.contact-info-panel::after {
  content: "";
  position: absolute;
  inset: 0;
  background-image: radial-gradient(rgba(255, 255, 255, 0.18) 1.4px, transparent 1.4px);
  background-size: 20px 20px;
  -webkit-mask-image: linear-gradient(160deg, black, transparent 65%);
  mask-image: linear-gradient(160deg, black, transparent 65%);
  pointer-events: none;
}
.contact-info-panel > * { position: relative; z-index: 1; }
.contact-info-panel-item {
  display: flex;
  align-items: flex-start;
  gap: 0.8rem;
  margin-top: 1.2rem;
  font-weight: 600;
  font-size: 0.92rem;
}
.contact-info-panel-item i {
  width: 26px;
  height: 26px;
  min-width: 26px;
  border-radius: 50%;
  background: var(--ro-ink);
  color: var(--ro-white);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.8rem;
}

/* ---------- Franja de estadísticas ---------- */
.stats-band {
  background: var(--ro-ink);
  color: var(--ro-white);
  padding: 3.2rem 0;
  position: relative;
}
.stats-band::before {
  content: "";
  position: absolute;
  inset: 0;
  background: var(--ro-gradient);
  opacity: 0.08;
}
.stats-band .stat-number {
  font-size: 2.3rem;
  font-weight: 800;
  color: var(--ro-amber);
}
.stats-band .stat-label {
  font-size: 0.85rem;
  color: rgba(255, 255, 255, 0.7);
}

/* ---------- Lightbox (ampliar / descargar fotos) ---------- */
.lightbox-modal-content {
  background: var(--ro-ink);
  border: none;
  border-radius: 18px;
  overflow: hidden;
}
.lightbox-image {
  max-width: 100%;
  max-height: 78vh;
  object-fit: contain;
}
.lightbox-modal-close {
  position: absolute;
  top: 1rem;
  right: 1rem;
  z-index: 3;
  opacity: 0.85;
}

/* Cursor y micro-interacción en cualquier foto clicable (lightbox-trigger) */
.lightbox-trigger {
  cursor: zoom-in;
  position: relative;
}
.lightbox-zoom-hint {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(20, 21, 26, 0);
  color: var(--ro-white);
  font-size: 1.6rem;
  opacity: 0;
  transition: opacity 0.25s ease, background 0.25s ease;
  pointer-events: none;
}
.lightbox-trigger:hover .lightbox-zoom-hint {
  opacity: 1;
  background: rgba(20, 21, 26, 0.35);
}

/* ---------- Galería ---------- */
.gallery-box {
  position: relative;
  width: 100%;
  aspect-ratio: 3 / 4;
  border-radius: 16px;
  overflow: hidden;
  background: var(--ro-gray-300);
  box-shadow: 0 10px 28px rgba(20, 21, 26, 0.1);
}
.gallery-box img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  transition: opacity 0.5s ease;
}
.gallery-box img.fading {
  opacity: 0;
}
.gallery-box-tall {
  aspect-ratio: 4 / 5;
}

/* ---------- Clientes ---------- */
.client-card {
  border-radius: 16px;
  padding: 1.6rem;
  background: var(--ro-white);
  border: 1px dashed var(--ro-gray-300);
  height: 100%;
}
.client-card .client-name {
  font-weight: 800;
  color: var(--ro-ink);
  margin-bottom: 0.4rem;
}

/* ---------- Acordeón de servicios ---------- */
.accordion-ro .accordion-item {
  border: 1px solid var(--ro-gray-300);
  border-radius: 14px !important;
  overflow: hidden;
  margin-bottom: 0.9rem;
}
.accordion-ro .accordion-button {
  font-weight: 700;
  background: var(--ro-white);
}
.accordion-ro .accordion-button:not(.collapsed) {
  background: var(--ro-gray-100);
  color: var(--ro-magenta);
  box-shadow: none;
}
.accordion-ro .accordion-button:focus {
  box-shadow: none;
  border-color: var(--ro-gray-300);
}
.accordion-ro .accordion-button::after {
  filter: none;
}

/* ---------- CTA de contacto ---------- */
.cta-band {
  background: var(--ro-dark-gradient);
  color: var(--ro-white);
  border-radius: 24px;
  padding: 3.2rem 2.6rem;
  position: relative;
  overflow: hidden;
}
.cta-band::before {
  content: "";
  position: absolute;
  inset: 0;
  background-image: radial-gradient(rgba(255, 255, 255, 0.16) 1.6px, transparent 1.6px);
  background-size: 26px 26px;
}
/* Resplandores de color eliminados: el fondo ahora es un color sólido, sin mezcla de tonos */
.cta-band .cta-content { position: relative; z-index: 1; }

/* ---------- Contacto ---------- */
.contact-info-item {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  margin-bottom: 1.4rem;
}
.contact-info-item .icon-wrap {
  width: 44px;
  height: 44px;
  min-width: 44px;
  border-radius: 12px;
  background: var(--ro-amber-dark);
  color: var(--ro-white);
  display: flex;
  align-items: center;
  justify-content: center;
}
/* Intercalar los colores corporativos vivos en cada ícono, en vez de fondo negro */
.contact-info-item:nth-of-type(1) .icon-wrap { background: var(--ro-amber-dark); }
.contact-info-item:nth-of-type(2) .icon-wrap { background: var(--ro-magenta); }
.contact-info-item:nth-of-type(3) .icon-wrap { background: var(--ro-purple); }
.contact-info-item:nth-of-type(4) .icon-wrap { background: var(--ro-amber); }
.form-control:focus, .form-select:focus {
  border-color: var(--ro-amber);
  box-shadow: 0 0 0 0.2rem rgba(245, 166, 35, 0.2);
}

/* ---------- Footer ---------- */
footer.ro-footer {
  background-color: var(--ro-ink);
  color: rgba(255, 255, 255, 0.7);
  padding: 1.4rem 0;
  border-top: 1px solid rgba(255, 255, 255, 0.08);
}
footer.ro-footer a { color: rgba(255, 255, 255, 0.7); }
footer.ro-footer a:hover { color: var(--ro-amber); }
.footer-row {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: space-between;
  gap: 1rem 1.5rem;
}
.footer-brand {
  display: flex;
  align-items: center;
  gap: 0.8rem;
  font-size: 0.82rem;
}
.footer-social {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  font-size: 0.82rem;
}
.footer-social-label { margin-right: 0.2rem; }
footer.ro-footer .social-icon {
  width: 34px; height: 34px; border-radius: 50%;
  display: inline-flex; align-items: center; justify-content: center;
  color: var(--ro-white);
  font-size: 0.95rem;
  transition: transform 0.2s ease, filter 0.2s ease;
}
footer.ro-footer .social-icon:hover { transform: translateY(-2px); filter: brightness(1.1); }
/* Colores de marca reales para cada red social */
footer.ro-footer .social-icon[aria-label="Facebook"] { background: #1877F2; }
footer.ro-footer .social-icon[aria-label="LinkedIn"] { background: #0A66C2; }
footer.ro-footer .social-icon[aria-label="Instagram"] {
  background: radial-gradient(circle at 30% 107%, #fdf497 0%, #fdf497 5%, #fd5949 45%, #d6249f 60%, #285AEB 90%);
}
.footer-legal {
  display: flex;
  align-items: center;
  gap: 0.6rem;
  font-size: 0.82rem;
}
.footer-legal-sep { color: rgba(255, 255, 255, 0.3); }
@media (max-width: 767.98px) {
  .footer-row { justify-content: center; text-align: center; }
}

/* ---------- Botón flotante de WhatsApp ---------- */
.whatsapp-float {
  position: fixed;
  bottom: 24px;
  right: 24px;
  z-index: 1050;
  width: 58px;
  height: 58px;
  border-radius: 50%;
  background: #25D366;
  color: var(--ro-white);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.6rem;
  box-shadow: 0 10px 25px rgba(37, 211, 102, 0.4);
  transition: transform 0.2s ease;
}
.whatsapp-float:hover { transform: scale(1.08); color: var(--ro-white); }

/* ---------- Modal de bienvenida ---------- */
.welcome-modal-content {
  background: var(--ro-ink);
  border: none;
  border-radius: 22px;
  padding: 1rem;
  position: relative;
  overflow: hidden;
}

.welcome-modal-content .modal-body {
  position: relative;
  z-index: 1;
  padding: 1.6rem 1.8rem 2rem;
  color: var(--ro-white);
}
.welcome-modal-close {
  position: absolute;
  top: 1.2rem;
  right: 1.2rem;
  z-index: 2;
  opacity: 0.8;
}
.welcome-badge {
  display: inline-block;
  background: rgba(255, 255, 255, 0.08);
  border: 1px solid rgba(255, 255, 255, 0.16);
  color: var(--ro-amber);
  font-weight: 700;
  font-size: 0.78rem;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  border-radius: 50px;
  padding: 0.4rem 1rem;
}
.welcome-pill {
  display: block;
  background: rgba(255, 255, 255, 0.06);
  border: 1px solid rgba(255, 255, 255, 0.14);
  border-radius: 12px;
  padding: 0.75rem 1rem;
  font-weight: 700;
  font-size: 0.88rem;
  color: var(--ro-white);
  text-decoration: none;
  transition: background 0.2s ease, border-color 0.2s ease;
}
.welcome-pill:hover {
  background: rgba(245, 166, 35, 0.14);
  border-color: var(--ro-amber);
  color: var(--ro-white);
}

/* Botón flotante que reabre el modal (junto al de WhatsApp) */
.welcome-float {
  position: fixed;
  bottom: 94px;
  right: 24px;
  z-index: 1040;
  background: var(--ro-btn-gradient);
  color: var(--ro-white);
  border: none;
  font-weight: 700;
  font-size: 0.85rem;
  border-radius: 50px;
  padding: 0.75rem 1.3rem;
  box-shadow: 0 10px 25px var(--ro-btn-shadow);
  transition: transform 0.2s ease, background 0.2s ease;
}
.welcome-float:hover {
  background: var(--ro-btn-gradient-hover);
  transform: translateY(-2px);
  color: var(--ro-white);
}

@media (max-width: 575.98px) {
  .welcome-float span { display: none; }
  .welcome-modal-content .modal-body { padding: 1.4rem 1.2rem 1.8rem; }
}

/* ---------- Responsive ---------- */
@media (max-width: 767.98px) {
  section { padding: 3.2rem 0; }
  .hero { padding: 3.2rem 0 2.4rem; text-align: center; }
  .hero-pills { justify-content: center; }
  .cta-band { padding: 2.4rem 1.6rem; text-align: center; }
  .stats-band .stat-number { font-size: 1.8rem; }
}
